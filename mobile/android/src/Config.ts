export const Config = {
  // The base URL for the backend API.
  // For production, this should point to the live server.
  API_BASE_URL: 'https://swiftpay.site/api/v1',

  // App display name
  APP_NAME: 'xend POS',

  // Build-time environment flag
  ENV: 'production',
};
