fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.5.0,<2.10
pydantic-settings>=2.2.0
email-validator>=2.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.6  # Required for FastAPI Form data handling
aiofiles>=23.2.1  # Required for FastAPI StaticFiles

# Development and testing
pytest>=8.4.1
pytest-asyncio>=1.1.0
httpx[socks]>=0.27.0  # HTTP client for SMS gateway requests
requests>=2.32.0  # Used by local smoke checks and service integrations

# ASW Lambda
mangum==0.19.0

# Crypto
cryptography
bcrypt>=4.0.0,<4.1  # passlib 1.7.4's bcrypt backend detection breaks on bcrypt>=4.1 (no `__about__`)


# database module dependencies
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
aiosqlite>=0.20.0
greenlet
xmltodict

# auth module dependencies
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# aihub module dependencies
openai>=1.0.0
sse-starlette>=1.6.0

# QR code generation
qrcode[pil]>=7.4.2

# Background job scheduler
apscheduler>=3.10.0

# QR code decoding (requires libzbar0 system library)
pyzbar>=0.1.9

# Ensure up-to-date CA certificates for SSL verification
certifi>=2023.7.22
